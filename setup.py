from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    description="""Extracting CSRD data from corporate reports using retrieval-
    augmented generation""",
    author="AI-in-Management",
)
